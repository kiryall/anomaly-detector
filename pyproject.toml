[project]
name = "anomaly-detector"
version = "0.1.0"
description = "Anomaly detection MVP"
requires-python = ">=3.11"
dependencies = [
    "nicegui>=3.15.0",
    "pydantic>=2.13.4",
]

[dependency-groups]
dev = [
    "ultralytics>=8.4.117",
]
